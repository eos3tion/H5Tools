/**
 * 箭头方向
 */
declare const enum ArrowType {
    /**
     * 自动判断方向
     */
    Auto = 0,
    Up = 1,
    Down = 2,
    Left = 3,
    Right = 4,
    /**
     * 派发事件
     */
    Event = 5
}

declare const enum ConstString {
    ProjectKey = "GuideTool"
}